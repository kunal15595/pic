

This  directory contains additional  utility files that may
assist in AVIX based application development. These utility 
files are controller (port) specific and thus not available
for every AVIX port.

For some  ports no additional utility files are offered and
this directory is empty.